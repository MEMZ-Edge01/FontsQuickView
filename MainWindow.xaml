<Window
    x:Class="FontsQuickView.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="字体速览">
    <Grid x:Name="RootGrid" Padding="16,8,16,16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1" CornerRadius="8" Padding="16,12">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="预览文字" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBox Text="{Binding SampleText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             PlaceholderText="输入要预览的文字..." FontSize="14" CornerRadius="4"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="4" Width="180">
                    <Grid><Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="字号" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Grid.Column="1" Text="{Binding PreviewSizeDisplay}" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </Grid>
                    <Slider Minimum="12" Maximum="80" Value="{Binding PreviewSize, Mode=TwoWay}"
                            SnapsTo="StepValues" StepFrequency="1"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Spacing="4">
                    <TextBlock Text="搜索字体" FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBox Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             PlaceholderText="输入字体名..." FontSize="14" CornerRadius="4"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16" Margin="8,2,0,2">
            <TextBlock Text="筛选：" FontSize="12" VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <CheckBox Content="全部字体" IsChecked="{Binding ShowAllFonts, Mode=TwoWay}" FontSize="13"/>
            <CheckBox Content="支持中文" IsChecked="{Binding ShowChineseOnly, Mode=TwoWay}" FontSize="13"/>
            <CheckBox Content="支持英文" IsChecked="{Binding ShowEnglishOnly, Mode=TwoWay}" FontSize="13"/>
        </StackPanel>

        <ListView
            Grid.Row="2"
            ItemsSource="{Binding Fonts}"
            SelectionMode="None"
            Padding="0,0,4,0"
            ScrollViewer.IsDeferredScrollingEnabled="True">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="0"
                                   CacheLength="2.0"
                                   ItemWidth="{Binding ItemWidth}"
                                   ItemHeight="{Binding ItemHeight}"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="MinHeight" Value="0"/>
                    <Setter Property="Margin" Value="0,0,6,4"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4,3" Spacing="2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontSize="11"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{Binding UnsupportedText}" FontSize="11"
                                       Foreground="#D9C123" Visibility="{Binding UnsupportedVis}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding DataContext.SampleText, ElementName=RootGrid}"
                                   FontFamily="{Binding FontFamily}"
                                   FontSize="{Binding DataContext.PreviewSize, ElementName=RootGrid}"
                                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"
                                   VerticalAlignment="Top"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8" Padding="12,6" Margin="0,0,8,8">
            <TextBlock Text="{Binding CountDisplay}" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>
    </Grid>
</Window>